/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package Examples;

import java.util.Scanner;

/**
 *
 * @author modia9548
 */
public class String2 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.println("Please enter a word");
        String word = input.nextLine();
        //see if word starts with a vowel
        if(word.startsWith("a") || word.startsWith("i") || word.startsWith("e") || word.startsWith("o") || word.startsWith("u"));
        //add "way" to the end of the word
        String translated = word + "way";
        System.out.println(word + " in pig latin is " + translated);
        //convert to lowercase
        word = word.toLowerCase();
        
        //find the vowel by going through each letter
        or(int i =0; i <word.length(); i++){
        //look for vowel at spot i
        if (word.charAt(i)== "o" || word.charAt(i)== "a" || word.charAt(i)=="e" || word.charAt(i)=="i" || word.charAt(i)=="u")
            //chop the word up at the vowel
            String front = words.substring(0, i);
        String  ending = word.substring (i);
        //assemble translation
        
    }
        //research regular expressions
    }
    //where can you find letter e
    int e = word.indexOf("i", 2);
    
    StringBuilder = new StringBuilder()
            
}
