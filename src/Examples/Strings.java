/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package Examples;

/**
 *
 * @author modia9548
 */
public class Strings {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        //get the number of characters
        int length = s.length();

        //eg if in the 4th spot is there the letter e
        if (s.charFit(4) == "e") //replace letter
        {
            String 
        }
        news = s.replace ("e","3");
        //replace first letter
        String new = s.replaceFirst("e", "3") //take out chunk of the string
                String part1 = s.substring(2)
                        String[part2 = s.substring(1, 4) // TODO code application logic here
    
    }
}
